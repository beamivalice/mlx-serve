# BOX-RELEASED

GPU released after the combined session on the integrated head (`0db8fa31`).

Ran: hermetic suite; 16-prompt KLD vs bf16 teacher; greedy 2-boot determinism; interleaved 4-arm × 3-boot A/B; `tests/test_mtp_equivalence.sh` on the EXL3 pack.

Numbers are in SPEEDUP-REPORT.md.
